// Dependencias
import Swal from "sweetalert2";
import { Task } from "./services/Api";

// Elementos HTML del DOM
const containerTasks = document.querySelector<HTMLTableSectionElement>("#container-tasks")!;
const formTask = document.querySelector<HTMLFormElement>("#form-task")!;

// Evento que pinta las tareas al momento de cargar la pagina
document.addEventListener("DOMContentLoaded", async () => {

});

// Se crea un unico evento para todos los botones de editar, eliminar y cambiar estado
containerTasks.addEventListener("click", async (e) => {

});

// Evento Submit del formulario
formTask.addEventListener("submit", async (e) => {

});



